services:
  app:
    build:
      context: .
      target: prod
    expose:
      - "8000"

  nginx:
    image: nginx:latest
    depends_on:
      - app
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
